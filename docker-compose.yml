version: '3.8'

services:
  calyth:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
